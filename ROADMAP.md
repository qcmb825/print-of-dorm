# 未来路线图

主线只有一件事：**让这个系统自己算得出该收多少钱、收得到钱、并且能把文件直接送到打印机上。**

现在的形态是「学生上传 → 管理员接单 → 管理员手工下载、打开、点打印 → 学生凭取件码取件」。
管理员在这条链条上做的全是机械动作，而「该收多少钱」根本没进系统——它靠事后口头算或者干脆不收。
这份路线图就是把这两件机械的事交给程序：**计价自动化、下发自动化**，钱的部分再往上接一层。

> 本文只讲**打算做什么、为什么、什么顺序**。当下已确认的短板在 README 的「已知不足与后续计划」里
> （那节末尾跟着这里的条目编号）。两边互相对齐：做完一项就删掉那边对应的条目。

## 怎么读这份文档

条目编号是**字母 + 序号**，字母对应阶段，跨文档引用时用编号（比如「见 ROADMAP C.2」）：

| 阶段 | 主题 | 说明 |
| :--- | :--- | :--- |
| **A** | 前提 | 不做这些，后面的全是空谈 |
| **B** | 自动计价 | 算得清 |
| **C** | 付款联动 | 收得到 |
| **D** | 打印机联动 | 打得动 |
| **E** | 闭环与异常 | 出错时谁负责 |
| **F** | 配套 | 服务于主线，但不阻塞它 |

同一个阶段内按重要性从上到下。字母不代表排期，A 是**唯一有强制顺序的阶段**——它里面的东西是
后面几步的技术前提，先做它是为了省掉返工。

---

## 目标形态

```
现状：上传 → 接单 → 手工下载/打开/点打印 → 手工判断收多少钱 → 取件
目标：上传 → 自动计价(页数×份数×单价) → 下单即知金额 → 付款 → 一键下发到打印机 → 取件核验
```

三个阶段的落地顺序是 **B → C → D**，但有一个前提性的判断要先说清楚：

> ### 关键判断：付款和打印不必强绑定
>
> 如果做成「无人值守自助打印」（学生自己取纸、自己走），那付款就是打印的**前置闸门**，
> 必须严丝合缝地卡在打印之前。
>
> 但已经确定走的是**管理端一键下发**，也就是说**有人值守**。有人值守时「先打印、取件时结算」
> 是现实里更常见的做法，于是**付款可以从打印链路里解耦出来**：先把金额算对、把账记清、
> 把流水和余额做出来，付款渠道什么时候接、接哪家，都不影响前面这些。
>
> 这条判断的价值在于：**它能让你把最不确定的那部分（支付资质、回调、对账）推迟到后面**，
> 而先把确定能做的（计价、下发）做完并投入使用。路线图就是按这个思路排的。

---

## A · 前提

### A.1 数据库迁移机制 ← 阻塞项，必须最先做

- **为什么它排在第一位**：接下来的每一步都要改表结构——加页数、加份数、加纸张规格、加金额、
  加价目表、加支付流水表。而现在**改 `orders` 的结构 = 丢订单**：
  `db.SCHEMA_VERSION` 只是个写在 `schema_meta` 里的标记，改它不做任何迁移；
  唯一真正的迁移路径是「`schema_meta.schema_version` 缺失时 `DROP TABLE orders` 重建」。
- **做法**：把它变成一串**有序、幂等、只前进不回退**的迁移步骤：
  - `schema_meta.schema_version` 记当前版本，每个迁移函数负责从版本 N 升到 N+1；
  - 启动时对比版本、依次执行、**每步单独提交**（中途失败不会留下半升级状态）；
  - 把 `DROP TABLE orders` 降级成「只对完全没有版本标记的史前库执行」，
    并且执行前**改名备份**（`orders_legacy_<时间戳>`）而不是直接删。
- **动手前**：备份数据库和上传目录（README 已提醒，这里再强调一次）。
- **验收**：造一个旧库，启动后数据还在、结构升到最新；重复启动不重复执行。

### A.2 金额与计价逻辑的自动化测试

- **为什么要提前做**：涉及钱的代码，没有测试就是在赌。**算错单价、重复扣款、并发下单超扣**，
  这三类 bug 一旦上线，损失是学生的钱和你的信誉，而且事后很难查（流水不全就还原不出经过）。
- **现状与难点**：后端一个测试都没有。`import app.py` 不是纯函数式导入——它会建库、建内置超管、
  写启动日志，测试直接 import 会碰到真实数据库。
- **做法**：
  - 起步够用：在 import 之前把 `DATABASE_PATH` / `UPLOAD_FOLDER` / `LOG_DIR` 指向临时目录
    （`config.py` 在 import 时就读这些环境变量，时序对了就自然隔离），再 `import app`；
  - 想更干净就抽 `create_app()` 工厂，`app.py` 只留调用和启动。
    **这会动 `app.py` 的结构，动手前先读它里面那几段中文注释**（端口自检、日志分级的实测结论都在那）。
- **重点覆盖**（按风险排序）：
  1. 计价：单双面 × 彩色黑白 × A4/A3 的金额，含奇数页双面、份数 > 1；
  2. 扣款：余额不足被拒、同一笔重复提交只扣一次、并发下单不会扣成负数；
  3. 越权与鉴权（现在最怕改坏的地方）：接单并发只有一个成功、管理员改他人订单拿到 403、
     `?with_password=1` 只有超管有效且普通管理员拿到的列表里没有超管；
  4. CSRF：写方法缺 `X-CSRF-Token` 返回 403。
- **验收**：`pytest` 一条命令跑完全绿，且不碰开发库、不写真实 `logs/`。

### A.3 页数解析

- **为什么是前提**：没有页数就没有计价。这是整条主线里**唯一一个「拿不到数据」级别的障碍**——
  份数、单价都能问人、能配置，页数必须从文件里读出来。
- **按文件类型分别看**（这里的差别很大，别当成一件事）：

  | 类型 | 能不能直接拿到页数 | 说明 |
  | :--- | :--- | :--- |
  | PDF | 能 | 读页树即可，**首选 `pypdf`**（纯 Python、BSD 许可、无原生依赖） |
  | 图片（jpg/png） | 能，恒为 1 页 | |
  | docx / doc | **不能** | Office 文件里根本没有可靠的页数——`docProps/app.xml` 里那个 `<Pages>` 是 Word 上次保存时写进去的，经常缺失或过期，别的工具生成的 docx 甚至没有。**要真页数就必须先转成 PDF** |

- **doc/docx 的转换**：用 LibreOffice 无头模式（`soffice --headless --convert-to pdf`）。
  代价要提前知道：安装包几百 MB、首次启动慢、**偶发卡死所以必须设超时**，
  而且**转换出来的页数未必等于实际打印的页数**（字体缺失会导致回流，排版跑掉）。
- **因此定调**：解析出来的页数是**预估值**，不是结算依据（见 B.4）。
- **解析失败必须有人工路径**：坏 PDF、加密 PDF、转换失败，都不能让学生下不了单。
  兜底是「标记为待人工确认，管理端接单时填页数」，而不是拒绝下单。
- **许可提醒**：别用 PyMuPDF——它是 AGPL，这个项目的 LICENSE 是 All Rights Reserved，
  许可上会打架。真需要更准的渲染再考虑 `pypdfium2`（Apache-2.0 / BSD-3）。
- **验收**：常见的 PDF / docx / 图片能读出页数；坏文件走人工路径；测试里有几个真实的样例文件。

### A.4 HTTPS 与域名备案

- **如果走微信/支付宝**：这不是优化项而是硬前提——**支付回调必须走 HTTPS 且域名已备案**，
  没有它连商户号都开不下来。
- **就算不走**：也该有。没有 HTTPS，登录凭据和会话 Cookie 在宿舍网里是明文跑的。
- **配套**（这三件事必须一起开、且顺序有讲究）：
  - `SESSION_COOKIE_SECURE=true` + `TRUST_PROXY=true` 一起开。
    **只开后者不开前者**会让日志里的 IP 全是代理的 IP，限流跟着失效；
    **只开前者不开后者**在代理后面登录态会直接失效。
  - 上传大小限制要在代理层同步放开，否则大文件在 Nginx 那层就被拦了，后端根本看不到请求。
  - 代理的超时时间要大于上传耗时。
- **验收**：照文档配完，登录正常，访问日志里的 IP 是真实客户端 IP。

---

## B · 自动计价

### B.1 把打印参数补全：份数、纸张规格

- **现状**：下单参数只有「黑白/彩色」和「单面/双面」。**要打三份怎么办？现在只能写进备注靠人看。**
- **要加的两个字段**：
  - `copies`（份数，整数，校验范围比如 1~99）
  - `paper`（纸张规格，`A4` / `A3`）
- **注意**：`orders` 的字段名是前后端手写对齐的，加字段时**两边一起改**，不然就是静默不渲染
  （`/api/admin/stats` 的 `top_claimers` 踩过这个坑）。加列走 A.1 的迁移机制，不要手写 `ALTER`。
- **验收**：下单填 3 份 + A3，学生端和管理端都看得到。

### B.2 价目表：落库、可改、且必须快照

- **计价单位是「张」**，价目表因此是 **8 个价目**：`{彩色, 黑白} × {单面, 双面} × {A4, A3}`。
  这个矩阵建议在数据库里存成一张 `price_rules` 表**而不是写在 `config.py` 里**——
  调价是运营动作，不该要求改代码重启服务。
- **谁能改**：超管。改价必须走 `audit_action` 留痕（记清改前改后、谁改的、什么时候）。
- **最关键的一条设计约束：订单必须在**下单那一刻**把单价快照下来。**
  否则一次调价会把所有历史订单的金额全部改写，账就再也对不上了。
  订单表里存 `unit_price`（下单时的单价），而不是只存一个外键去引用价目表。
- **验收**：改价之后，历史订单的金额不变，新订单按新价。

### B.3 计费规则

```
金额 = 张数 × 份数 × 单价(颜色, 单双面, 纸张规格)
```

**张数怎么算**：

| 单双面 | 张数 | 说明 |
| :--- | :--- | :--- |
| 单面 | 页数 | 一张纸印一面 |
| 双面 | `ceil(页数 / 2)` | **奇数页时最后一张纸只用了一面，纸照样消耗了，仍按张收费** |

这就是「双面按张而不是按页」的落点：双面省的是纸，收的也是纸的钱。

- **金额一律用整数「分」存**（`INTEGER`）。SQLite 没有 decimal，`REAL` 是 IEEE754，
  用浮点存钱迟早出现 `0.1 + 0.2 != 0.3` 这类问题，对账时非常难看。
- **计价口径必须和打印设置一一对应**：任何「N-up / 缩放 / 每张纸印多页」的选项一旦加上，
  张数算法就要同步改，否则**算出来的钱和实际用纸对不上**。这条写进代码注释，别让下一个人踩。
- **混合尺寸的文件**（一个 PDF 里既有 A4 又有 A3 页）现在没法准确计价——口径假设整份文件同一规格。
  这种单子走人工定价，别硬算。
- **验收**：单面 3 页/双面 3 页/A3/多份的组合都有测试覆盖，且测试里写明期望值的算法。

### B.4 预估值与实际值：两套金额

- **为什么必须分两套**：A.3 的页数是预估值，doc 转 PDF 会跑版、字体缺失会回流；
  学生上传的和实际打出来的页数可能不一样。**只有一个金额字段，早晚会出现「收的钱和用的纸对不上」
  又说不清是哪一步错的局面。**
- **做法**：
  - `orders` 存两个金额：`quoted_amount`（下单时的预估，给学生看）和 `final_amount`（结算用）；
  - **最终定价的时机放在管理端接单时**：管理员看到实际页数（也可以手动改），确认后金额定下来，
    这个动作留痕。这一步同时是「人工修正解析错误」的入口，正好补上 A.3 的兜底；
  - 学生下单时只看到预估价，且**页面上要说清「最终以实际页数为准」**，否则取件时金额变了会变成投诉。
- **更简化的备选**（如果你觉得两套金额太重）：**接单时才计价，学生下单页只显示「待定价」**。
  少一个字段、少一套展示逻辑，代价是学生下单时不知道要花多少钱。**按目前有人值守的形态，
  这个备选其实是够的**，可以先走这条，以后再补预估价。
- **验收**：接单改页数后金额跟着变，且预估和最终两个值都能在接口里看到。

### B.5 下单页展示预估金额

- 单价、张数、份数、总额逐项列出——**不要只给一个总数**。学生要能看懂它是怎么算出来的，
  否则每一次金额争议都要人来解释一遍。
- 前端算的金额只是显示，**以后端返回的为准**（前端的校验永远是体验，不是安全边界）。
- **验收**：改份数/规格时金额实时更新，且与后端返回一致。

---

## C · 付款联动

### C.1 支付状态必须独立于打印状态

- **不要往 `ORDER_STATUSES` 里塞支付状态。** 那个元组是**前后端共用的展示标识**，
  前端按它取状态色（`src/utils/format.ts` 的 `STATUS_COLOR_VAR`），
  塞进去会让「待打印」和「未支付」这两个正交的维度搅在一起，状态色也没法表达。
- **正确的做法是两条独立的轴**：

  | 轴 | 取值 | 谁在动 |
  | :--- | :--- | :--- |
  | 打印状态 | 待打印 / 打印中 / 可取了 / 已取件 | 接单人手动切换 |
  | 支付状态 | 未支付 / 已支付 / 已退款 / 已冲正 | 支付回调或管理员 |

  界面上把它们**分开显示**（比如订单卡片上两个标签）。某些非法组合要在后端挡掉
  （比如「已取件 + 未支付」要不要允许？**这个必须明确**——它决定了有没有赊账）。
- **验收**：能独立改支付状态而不影响打印状态，反之亦然。

### C.2 余额与扣款的原子性

- **如果做余额**（渠道确定前的推荐起点，见 C.4），扣款只有一种正确写法：

  ```sql
  UPDATE users SET balance = balance - :amount
  WHERE id = :uid AND balance >= :amount
  ```

  再看 `rowcount`：为 0 就是余额不足。**绝不能「先 SELECT 查余额、再 UPDATE 扣减」**——
  两个请求同时通过检查就会扣成负数，这是最经典的超扣漏洞。
- **扣款和订单必须在同一个事务里**。分开写就会出现「扣了钱没订单」或「有订单没扣钱」，
  而且这两种情况都只在异常路径下发生，等发现时已经过去很久了。
- **SQLite 的一个坑**：SQLite 是单写者，默认的延迟事务在读锁升级为写锁时可能直接返回
  `SQLITE_BUSY`（不遵守 `busy_timeout`）。**涉及余额的事务用 `BEGIN IMMEDIATE` 开头**，
  一开始就拿写锁，避免升级失败。现在 `db.get_db()` 只设了 `timeout=10`，不够。
- **验收**：并发扣款测试——同一账号同时发多笔扣款，余额不会变负，且成功笔数与余额减少额对得上。

### C.3 流水、对账、退款

- **流水表不可省**。每次余额变动/收款都写一条 `payment_records`（谁、多少、关联订单、渠道、
  渠道流水号、时间、状态）。订单表上的金额是**当前状态**，流水才是**经过**——
  出现争议时能还原的只有流水。**流水只增不改不删。**
- **幂等是硬要求**：支付渠道会重复回调（微信/支付宝都会）。用「渠道流水号」建唯一索引，
  重复回调直接返回成功但不重复记账。**没有幂等，一次重试就是多收一次钱。**
- **对账**：每天拉一次渠道账单，找出两类差异——
  ①本地已支付、渠道没确认；②渠道收到钱、本地没收到回调（回调丢了）。
  第二类必须有**主动补单**的手段，不能只等回调。
- **退款**：退款不是把金额改成 0，而是**新增一条反向流水**，原记录不动。退款要不要能退到余额、
  还是必须原路退回，取决于渠道（余额可以直接加回，微信支付要调退款接口且有时间限制）。
- **验收**：重复回调只记一次账；能导出一份用于对账的流水（含 BOM，否则 Excel 打开中文乱码）。

### C.4 渠道适配层（渠道待定，先留接口）

- 支付渠道本身**已经决定以后再定**。这个决定可以推迟，但代码结构不能等——
  一旦把「微信支付」写进订单流程里，以后换渠道就要重写业务逻辑。
- **做法**：把「渠道无关」和「渠道相关」切开：
  - **与渠道无关**（先做）：金额、支付状态、流水、余额、幂等、对账报表；
  - **与渠道相关**（后做）：发起支付、接收回调、查单、退款，收敛成一个薄的适配器，
    每个渠道一个实现，业务层只调接口。
- **推荐的起步方式**：**余额 + 管理员充值**。它是**唯一不需要任何外部资质和回调**的方案：
  管理员线下收到钱（现金/转账）后在后台给学生账号充值，学生下单直接扣余额。
  用它能把「计价 → 扣款 → 流水 → 对账」整条链路先跑通并投入使用，
  之后接真支付时只是多一个「充值入口」和一套适配器。
  > 这条是**建议**不是决定：它隐含了「先充值后消费」的产品形态。
  > 如果希望学生扫码即付不预存，那余额就不是起点，得直接上渠道——那要先把 A.4 做完。

- **验收**：业务层代码里搜不到任何渠道名字（全在适配器里）。

### C.5 支付资质与合规

- 涉及真实货币之后，这些是**非技术但会卡住技术**的事，需要提前确认而不是写代码时才想：
  - **能不能拿到商户号**：微信支付/支付宝需要企业或个体户资质。个人身份通常拿不到，
    这直接决定 C.4 走哪条路。
  - **退款规则**：多扣了、打废了怎么退，多久内必须退，这些要有文字约定。
  - **留痕要求**：钱相关的操作（充值、退款、改价、冲正）**全部**要 `audit_action` 留痕。
- **验收**：写在文档里，不是记在某个人脑子里。

---

## D · 打印机联动（管理端一键下发）

### D.1 部署前提

- **服务器必须能碰到打印机**。现实的组合是：
  - 服务跑在一台 Windows 机器上，机器上装了打印机驱动 → 用 **Windows 打印队列**（最省事，推荐）；
  - 或者服务跑在 Linux 上 → 走 CUPS，用 `lp` 下发（多一层权限和驱动的坑）。
- **注意 `UPLOAD_FOLDER` 是按启动时的当前目录解析的**（见 README），
  而打印下发要读文件，所以**工作目录必须固定**，否则换了启动方式就找不到文件。
- **验收**：在一台干净的部署机上照文档能配通，不用回头改代码。

### D.2 下发方式

- **推荐 SumatraPDF 的命令行**，它是这类无人值守 PDF 打印最成熟的方案，一个可执行文件、不需要装 Office：

  ```
  SumatraPDF.exe -print-to "打印机名" -print-settings "duplexlong,2x,paper=A4" -silent 文件.pdf
  ```

- **不要用 PowerShell 的 `Start-Process -Verb PrintTo` / `Out-Printer`**：
  它们依赖文件关联和默认程序，机器上装了什么、谁点过「默认打开方式」，行为就跟着变。
  这类问题在现场极难排查，而且换一台机器就复现不出来。
- **打印机的选择**：彩色/黑白在 Windows 上最稳的做法是**预先配好两个逻辑打印机**
  （一个默认黑白、一个默认彩色），下发时按订单选打印机名，
  而不是指望命令行能改驱动里的颜色模式——那取决于驱动，不通用。
- **验收**：一个 PDF 能按指定的单双面、纸张、份数打出来，且管理员全程不用点开文件。

### D.3 「已投递」不等于「打印成功」

- **这是整个 D 阶段最容易出错的地方**，必须一开始就把语义定清楚：
  **打印任务投递到队列后，程序返回的成功只代表「已交给打印机」，不代表纸出来了。**
  缺纸、卡纸、离线、驱动报错，本地都可能是「成功」的。
- **所以**：
  - 接口的返回文案是「已下发」而不是「已打印」，**界面上的措辞也要一致**——
    否则管理员会以为点了就完事，实际上一张纸都没出；
  - 状态**不自动跳到「已取件」**，人工确认这一环留着（这跟 A.3、B.4 里「机器算的都要人来确认」是同一个原则）；
  - 想更准可以查打印队列状态（WMI 的 `Win32_PrintJob` / `Win32_Printer`），
    但要清楚它**跨驱动不可靠**，只能当参考，不能当开关。
- **失败要能重试**：任务投递失败（打印机不在、路径不对）必须把错误原文记进日志，
  并且允许重试——**重试必须防重复**（见 D.4）。
- **验收**：把打印机断电再下发，界面明确显示「下发失败」而不是「成功」。

### D.4 防重复打印与串行化

- **防重复是第一位的**：重复打印浪费的是纸和钱，而且学生拿到两份会以为系统坏了。
  - 每次下发写一条 `print_jobs` 记录（订单号、打印机、参数、投递时间、结果、操作人）；
  - 同一订单**在不处于「打印中」的前提下不允许再次下发**，重复点击走确认弹窗而不是直接拦；
  - 这些动作全部 `audit_action` 留痕。
- **串行化**：打印队列本身是串行的，但**不要在请求线程里做阻塞的打印投递**——
  waitress 只有 8 个线程，几个人同时点就把线程占满了，整个服务会卡住。
  做法是在进程内维护一个**单线程的打印 worker**（一个队列 + 一个消费线程），
  接口只负责入队并立刻返回，前端轮询任务状态。
- **顺带的好处**：串行化之后「同一订单被下发两次」在结构上就很难发生。
- **验收**：并发点 5 个订单的下发，服务不卡、每单只打一次。

### D.5 打印参数映射

- 订单里的 `color_type` / `duplex` / `paper` / `copies` 要映射成下发参数，
  这张映射表要和 B.3 的计价口径**严格对应**——
  **计价按下发参数算，下发按下发参数打，两者差一个都会变成「收的钱和打的纸不一样」。**
  建议在代码里放在一起，并写一句注释说明这个约束。
- 纸张、单双面、份数能通过参数传；**颜色模式多半不行**（见 D.2 的双逻辑打印机做法）。
- **验收**：B 阶段每种计价组合，在 D 阶段都能打出对应的实物参数。

### D.6 命令行注入防护

- 打印下发要拼命令行，**这是这个项目里唯一一处「把数据交给外部程序」的地方**，所以特别说一下：
  - 用 `subprocess.run([exe, '-print-to', printer, path], shell=False)` **列表传参**，
    绝不用字符串拼接、绝不开 `shell=True`；
  - **落盘路径是安全的**：文件以 `uuid4().hex` 重命名，路径里没有用户可控内容；
  - **但展示用的原始文件名是用户可控的**（`orders.filename` 是上传时的原名），
    **绝对不要让它进命令行**。想给打印任务起个好认的名字，先按白名单过滤/截断再说。
  - 打印机名来自配置（可信），但仍然走同一个列表传参的口径，不搞例外。
- **验收**：用一个名字里带 `&` `|` `"` 的文件上传下单，下发时不会触发任何额外命令。

---

## E · 闭环与异常

### E.1 取件码核验

- **现状**：取件码已经生成了，但它只是个**给人看的数字**——置为「已取件」还是靠管理员手点，
  谁取的、码对不对，系统不认。付款接入之后，取件这一步就是收尾的对账点，值得做实。
- **做法**：加 `POST /api/order/<id>/pickup`，管理员输入学生出示的取件码，码对得上才置为「已取件」，
  记下操作人和时间；**码错也要留痕**（可能是猜码，属于安全事件）。
- **别假设取件码一定是 4 位数字**：`generate_pickup_code` 在连续 50 次都撞码后会退化成
  **6 位十六进制**（`utils.py` 里的兜底，为的是不让下单卡住）。核验逻辑和前端输入框都要按
  「不定长字符串」处理——写成 `len == 4` 或只允许数字，会在极端情况下把正常订单挡在外面。
- **顺带**：4 位数字在下单量大了之后碰撞概率不低，而 `generate_pickup_code` 每试一次查一次库，
  量大时会变慢。要上量就得重新掂量位数和唯一性策略。
- **验收**：码对上→已取件且留痕；码错→拒绝并写 `security.log`。

### E.2 打废、缺纸、卡纸：谁负责

- 这是**流程问题不是技术问题**，但必须在做 D 之前有答案，否则第一次出事故就会扯皮：
  - 打废了（打印机的问题）→ 谁承担成本？学生重打要不要再付一次？
  - 学生自己选错了参数（比如该双面选了单面）→ 已经印出来了，算谁的？
  - 打了但学生不来取 → 纸张占用，要不要清理（见 F.3）？
- **建议的默认口径**（要你们确认，不是代码能决定的）：**机器故障导致的作废不收费、可免费重打；
  参数由学生选定并确认过的，按实际用纸收费。** 关键在于**下单页要把参数写清楚让学生确认**，
  这是以后划责任的依据。
- **验收**：这三种情况在文档里有明确结论，且界面上有对应的操作入口（重打/免单/退款）。

### E.3 退款与冲正的具体流程

- C.3 讲了技术和记账方式，这里讲**操作流程**：谁能发起、要不要审批、多久内必须处理完。
- **余额最简单**：直接加回余额，一条反向流水就完了。
- **走渠道就麻烦**：要有退款时限（渠道一般限制可退时间窗口）、要处理「部分退款」、
  还要考虑渠道手续费谁承担。
- **建议**：**超管才能退款，且必须填原因**，原因进审计日志。
- **验收**：退款动作两步确认（避免误点），留痕完整。

### E.4 状态流转的服务端约束

- **现状**：后端只校验状态取值合不合法，**不校验顺序**，可以从「待打印」直接跳到「已取件」。
  README 明确写了「上图是操作约定，不是系统强制的状态机」。
- **为什么现在值得做**：接了付款之后，「已取件」意味着这笔账结清了，
  而**越级跳状态会让付款和取件的对账错位**。手工操作留下的脏数据，在对账时就是一笔查不清的差额。
- **做法**：把允许的流转写成一张表（`待打印 → 打印中 → 可取了 → 已取件`，外加取消这条支路），
  后端按表校验，越级返回 400。**给超管留一个显式的越级口子**（比如带 `?force=1` 且强制审计）——
  否则出异常时只能去改数据库，那比放开顺序更危险。
- **验收**：待打印直接改已取件被拒；超管带 force 能改且留痕。

### E.5 学生自助取消订单

- **现状**：提交完只能等，想取消得线下找管理员——但**「释放」只解绑接单人、不改状态**，
  取消这件事语义上根本没人管。
- **做法**：`POST /api/order/<id>/cancel`，**只允许本人、且只在还没被接单时**取消。
  已接单的要走工单沟通，别让「管理员打了一半你把它撤了」。
- **和付款的关系**（这里最容易出错）：
  - **未支付时取消**：直接关掉订单，不涉及钱，最简单；
  - **已支付时取消**：必须**同时触发退款**，而且要在同一个事务里标记，不能出现
    「订单取消了但钱还在」；
  - 已打印（打印中/可取了）不给取消，走 E.2 / E.3 的流程。
- **数据怎么处理**：别急着删。建议加一个「已取消」状态或 `cancelled_at` 标记。
  注意状态字符串是前后端共用的标识，加值的话 `config.ORDER_STATUSES`、
  前端的 `ORDER_STATUSES` 和 `STATUS_COLOR_VAR` 要同步，**漏了会是静默不渲染**。
- **验收**：取消自己未接单的订单成功；取消别人的、已接单的、已支付的，分别返回 403/409 或走退款。

---

## F · 配套

服务于主线，但不阻塞主线。**注意 F 不等于不重要**——其中几条是「现在没出事只是因为用量小」。

### F.1 通知与提醒

- **现状**：没有推送。学生端订单页 20 秒轮询一次，管理端订单页 10 秒轮询一次。
- **接了付款之后它变成必需品**：付款结果、余额不足、可以取件，这些都是**有时效性**的信息，
  靠学生自己刷页面是不行的。
- **做法**（按性价比排序，**不引入 WebSocket**）：
  - **站内通知中心**最实在：加 `notifications` 表（收件人、类型、关联订单、已读时间），
    布局层给个铃铛 + 未读数。**轮询一个轻量的未读数接口**比轮询整张订单列表便宜得多；
  - **浏览器通知**（Notification API）能加，但必须**默认关闭、显式授权**，
    被拒之后不能反复弹窗骚扰；
  - 邮件/短信需要外部服务，属于「配置了才启用」的可选项。
- **为什么不上 WebSocket**：现在是 waitress 8 线程的单进程，长连接会把线程吃满，
  一个安静的页面就能占住一个 worker。要上长连接得先换服务器模型，那是另一个量级的事。
- **验收**：付款成功/可以取件后，对应的人不刷新页面也能看到未读提示。

### F.2 `/api/my-orders` 分页与筛选

- **现状**：管理端订单列表已经支持分页（默认 20、上限 100），学生查自己订单的接口**一次全返回**。
  用两年的账号就是几千条一次性塞进浏览器。
- **做法**：照抄 `/api/orders` 的分页写法（`positive_int` + `LIMIT/OFFSET` + `total`），支持按状态筛选。
- **注意**：页面是 20 秒轮询整表重拉的，分页后**别让轮询把页数重置回第一页**。
- **验收**：超过一页时只返回一页且 `total` 正确；轮询不会把用户翻到的第二页弹回去。

### F.3 订单与文件的清理与孤儿扫描

- **现状**：订单只增不减，上传目录只增不减。另外写库失败时虽然会删掉刚落的文件，
  但**历史上已经产生的孤儿文件没人管**。
- **做法**（分两步，别一次做太满）：
  1. **先给管理员一个手动开关**：按「已取件超过 N 天」筛选并清理，动手前先告诉人
     「将删除多少条、释放多少 MB」，动作走 `audit_action` 留痕；
  2. **再考虑自动化**：没有常驻定时进程的前提下，可以用「启动时跑一次 + 每天第一次请求跑一次」
     这种土办法，或交给部署机的计划任务。
- **红线**：**默认不动手**。保留天数必须显式配置（`ORDER_RETENTION_DAYS`），默认值建议是「不删除」。
  **而且接了付款之后，已支付的订单还牵着流水**——删订单前必须确认对应的流水和金额有独立保留，
  否则对不了账。**有流水的订单只能标记归档，不能物理删除。**
- **顺手加**：「扫描孤儿文件」的管理动作——库里的路径不存在、或目录里的文件在库里查不到，
  都列出来给人看。这类问题现在只有等到有人点下载才发现。
- **验收**：能算出将清理多少、留痕、默认配置下什么都不删、有流水的订单删不掉。

### F.4 上传接口限流 + 限流计数落库

- **现状**：上传只有「扩展名白名单 + 大小上限」，登录/注册有失败计数，上传没有。
  而且那个计数存在**进程内存**里（`security._login_failures`），多进程部署时各算一份，阈值被成倍放大。
- **做法**：
  - 上传限流复用 `login_blocked` / `record_login_failure` 那套，阈值写进 `config.py`
    并在 `.env.example` 补说明；超限返回 429 且 `security_event` 留痕。
    **上传被限流是正常用户也会踩到的路径**，`msg` 要说清「多久后重试」，别只丢一句「操作失败」。
  - 计数挪进一张 `login_failures` 表（SQLite 就够，**不用为此引入 Redis**）。
- **顺带修一个不一致**：`login_key()` 用的是 `request.remote_addr`，没走 `client_ip()`。
  挂了反代且开了 `TRUST_PROXY` 时，登录计数用的 IP 和访问日志里的 IP 不是同一个，排查会看糊涂。
  统一成 `client_ip()`。
- **验收**：阈值调到 2 后连传 3 个文件，第 3 个拿到 429 且 `security.log` 有记录；
  两个进程也能正确累计失败次数。

### F.5 部署加固与备份

- **部署方式收敛**：给一份 nssm 或计划任务的**可复制**示例（含工作目录、重启策略、日志重定向）。
  **工作目录必须是项目根**——`UPLOAD_FOLDER` 按 CWD 解析，跑错目录会让老订单的文件路径全部失效。
- **健康检查**：现在只有 `/hello` 返回一句中文，它**不碰数据库**，数据库挂了它照样是好的。
  要让监控反映真实状态，得有个会真的 `SELECT 1`、并检查上传目录可写的探针。
- **备份**：一个脚本，把 SQLite 文件 + 上传目录打成一个带日期的包。
  **注意别直接复制正在写入的 `.db` 文件**——用 SQLite 的 `VACUUM INTO` 拿一致性快照，
  否则可能备份到一个损坏的库。接了付款之后，**流水和数据库的备份就是账**，这件事的优先级跟着上升。
- **验收**：备份包能在空目录里恢复出可启动的服务。

### F.6 前端欠账

- **窄屏触控目标**：组件尺寸沿用 Naive UI 默认高度（中号 38px），手机上按钮偏小。
  要补得覆写 `heightMedium` / `heightLarge` 这类**组件库级令牌**，别只在个别按钮上改
  （旧前端有一版，重写时没带过来）。
  **注意管理端会越来越多地在手机上用**（接单、点下发、填取件码），这条的实际影响在变大。
- **图表体积**：ECharts 单页约 200KB gzip。已经懒加载、学生端不下载，所以**优先级不高**；
  真要动就换更轻的图表库或手写 SVG，但注意图表颜色是从令牌反读的 hex（canvas 用不了 `var()`），别改丢。
- **验收**：手机上进管理端，按钮点得中；在真实设备上确认一次，别只在 DevTools 里切窄屏。

---

## 候选池（想到过，但没排期）

不进主线的原因写在括号里。要用的时候从这里捞，别让它们悄悄溜进代码。

- **全自动无人值守自助打印**（付款后自动打印、学生自取）——
  **当前明确选了「管理端一键下发」**，所以不在这条线上。它需要自助终端、出纸管控、
  缺纸/卡纸的无人处理方案，**而且付款要变成打印的硬前置**（C 阶段的形态会不一样）。
  真要走这条路，A/B/C/D 大部分能复用，但 E 阶段的异常处理要重写。
- **数据导出（订单/账号 CSV）**——对账要用的那份已经在 C.3 里了，其余的是运营便利。
  做的时候记得**带 BOM**（否则 Excel 打开中文乱码）和**防 CSV 公式注入**
  （备注是用户可控的，以 `=` `+` `-` `@` 开头会被 Excel 当公式执行，这是表格版的存储型 XSS）。
- **看板增强**（平均处理时长、时段分布、超时未接单提醒）——
  技术前提是要先加 `ready_time` 字段：**别拿 `update_time` 算完成时间**，
  它被改状态、释放等各种操作刷新，算出来是错的。
- **公告的定时与过期**——判断放在读取侧的 `WHERE` 里，不要靠改 `is_active`（那需要一个常驻进程去改）。
- **账号自助改密码**——改密码简单，但**找回需要一个可信的送达渠道**（邮箱/短信），那是外部依赖。
- **批量接单 / 批量下发**——高峰期一屏一屏点太慢，等订单量真的上来了再说。
- **限流与会话的共享存储（Redis）**——只有真要上多进程/多机时才需要。
  在那之前 F.4 用 SQLite 就够，多引入一个常驻服务不划算。
- **QQ 机器人（下单入口 + 通知推送）**——和上一条同类的理由：**要引入一套常驻的外部服务**。
  机器人本体是一个进程（NoneBot2 / Koishi 之类），QQ 协议端再反向 WS 连进来，
  对现在这个 waitress 单进程来说是一整套新的运维面。
  - **先选路线，两条都有硬伤**：社区现役的协议端是 **NapCat / Lagrange**（OneBot v11，go-cqhttp 已停维），
    功能全，但走的是模拟客户端，**有封号风险**，得拿小号跑；官方 QQ 机器人开放平台合规，
    但群/私聊能力要审核，且**主动消息有条数上限**——「通知推送」恰恰是主动消息，
    **这条限制会直接卡住用途**。上层别手搓协议，用现成框架，只写业务插件。
  - **下单入口必须复用已有的上传链路**：类型白名单、大小上限、限流都照 `/api/upload` 那套来，
    别在机器人侧开一个绕过校验的后门。另外 NapCat 的文件消息段**只给文件名、不给下载 URL**，
    要另调 `get_group_file_url` / `get_private_file_url` 才拿得到文件。
  - **身份绑定是这里最需要设计的部分**：「QQ 号 ↔ 账号」绑定表意味着又一个 A.1 迁移，
    而且**绑定必须可验证**——光凭在群里报一句学号就绑上的话，谁都能冒名下单、看别人的取件码。
    可参考取件码的思路，或走「网页里登录后生成一次性绑定码，再发给机器人」。
  - **和「明确不做」有张力**：如果做它的理由是「学生懒得开浏览器」，那正好撞上前面
    「移动 App / 小程序——Web 已经响应式了」那条。机器人真正不可替代的是另外两点：
    **文件本来就在 QQ 里**（不用先存到手机再上传）、以及**通知能主动触达**；
    这两点不成立的话，F.1 的站内通知中心就够了。它依赖 F.1 先有事件可推。

---

## 明确不做

写下来是为了省掉每次都重新讨论一遍的力气。

- **多租户 / 多打印点**：这是宿舍里的一套服务，不是 SaaS。要做就得先重想权限模型。
- **移动 App / 小程序**：Web 已经响应式了，宿网里打开浏览器就能用。
  （如果将来转自助打印，自助终端上的界面是另一回事，到时候单独评估。）
- **微服务拆分 / 消息队列**：单机单体是**特性**不是缺陷，拆了只会更难部署。
  打印任务的串行化（D.4）用一个进程内队列就够，不需要消息队列。
- **为了「现代」而上异步框架 / ORM / 连接池**：现在的同步 WSGI + 标准库 `sqlite3` 是刻意的选择
  （`requirements.txt` 里写了理由），低并发下它更简单也更稳。
- **第三方登录**：宿舍场景下学号就是身份，接 OAuth 是多此一举。
- **给超管看的明文密码再往外扩**：这个功能**存在**（`password_enc`），但方向是反的，
  见下一条。

---

## 需要拍板的问题

这两件事**不该由实现者单方面决定**，所以没有写进上面的阶段，列在这里等确认。

### 一、支付渠道（已决定「以后再定」，但有个前提要现在确认）

渠道细节可以推迟，**但有一条现在就得知道**：微信支付/支付宝需要**企业或个体户资质 + 已备案域名**。
如果这两样都拿不到，那 A.4 和 C.4 里走渠道的路就是走不通的，
**主线只能走「余额 + 管理员充值」**，C.4 的适配层也就暂时不用预留。
这直接决定 C 阶段要做多少工作，所以最好早点确认，而不是写到一半才发现。

### 二、`password_enc` 的存废

现在每个密码存两份：`password_hash`（pbkdf2，登录用）和 `password_enc`（Fernet，超管可解密查看）。
README 自己写了代价：**数据库泄露 = 全站明文密码泄露**，而密钥和数据库在同一台机器上，
挡不住拿到整机的人。考虑到很多人会在多个网站复用同一个密码，这个代价比看上去大。

这件事当初是需求方要求的，所以它是一个岔路口而不是待办：

| 选项 | 代价 |
| :--- | :--- |
| 保持现状 | 继续承担「库泄露 = 明文泄露」，但超管能帮学生找回密码 |
| 停止写入 `password_enc`，老数据保留 | 新账号不再可逆存；老账号仍暴露。**过渡期两种行为并存，最容易出错** |
| 彻底移除，改为「超管重置密码」 | 最安全；超管不能再查看原密码，只能生成新的给对方。**这是一次产品行为变更，要提前说清** |

建议方向是第三条，但**必须由需求方确认后再动**。

---

## 维护约定

- 做完一项：**勾掉，并补一句实现落在哪个提交/文件**，别让文档和代码漂移。
- 新增想法：先进「候选池」，**别直接改这条线**——路线图一膨胀就没人看了。
- 改 `AGENTS.md` / `README.md` 里和这里重叠的部分时，两边一起看：
  特别是 README 的「已知不足」，这里做完一项，那边对应的条目就该删掉。
- 这里提到的每个方案，动手时都要遵守 `AGENTS.md` 里的分层与约定
  （配置进 `config.py`、日志不用 `print`、权限收口在服务端、字段名前后端一起改）。
- **钱相关的改动一律按最严的来**：整数存分、流水只增不改、动作全留痕、异常路径要有测试。
