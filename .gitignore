# AI 工具的本地目录
.claude

# 本地工作目录：待办、报告、部署笔记和测试脚本，属于个人资料，整个目录不进仓库。
# 里面按用途分了子目录（memory/ tests/ tools/ probes/ docs/ artifacts/ runtime/），
# 分法见该目录自己的 README.md。
memoryandtest/

# 敏感资料目录：生产环境 .env（真实密钥）、从校内系统导出的学号姓名源文件。
# 整块忽略，不逐个列文件名 —— 以后往里放新东西不必记得回来补规则，
# 而漏一次的代价是把别人的个人信息推到公开仓库上，收不回来。
secrets/

# 生产环境回传的调试包（2026-09-23 起）：现场的 nginx 站点配置、.env 相关项清单、
# 路由器端口映射与 NAT 记录，以及待应用的补丁。里面是内网拓扑、域名/证书路径和
# 配置项清单 —— 属于部署细节，推上公开仓库等于把生产环境的地图发出去。
# 整块忽略：以后往里放新一版回传包不必记得回来补规则。
# 注意 *.md 只挡得住里面的说明文档（说明.md），zip 本身要靠这一条。
debug回传/

# 正在本机跑着的那份 LLOneBot 安装。它带 auth_token.txt / webui_token.txt /
# 每个 QQ 的 config_<QQ>.json / 登录态数据库，全是凭据与机器状态，整块不进仓库。
#
# 注意区分：**仓库里那份在 vendor/llonebot/**，是上游发行包（zip），不含任何凭据，
# 那是特意提交进去的 —— printbot 要跑起来就得有它，而部署机上不一定连得上下载源。
LLBot-CLI-win-x64-v8/

# 学生身份名单：从校内系统导出，是几万人的学号 + 姓名，属于他人个人信息。
# accounts.jsonl 是导出源文件，里面**还带着明文密码**；
# roster.db 是从它抽出来的「学号 + 姓名」名单库（由 memoryandtest/build_roster_db.py 生成，
# 注册校验时按学号去查它）。两者都只留在磁盘上、由人工拷到服务器，绝不进仓库 ——
# 这个仓库是公开的，一旦推上去就等于把几万人的姓名和学号公开了。
#
# roster.db 平时落在 data/ 下，已经被上面那条 data/ 规则挡住了；
# 这里仍显式写一条，是因为 ROSTER_DB_PATH 可以被配到 data/ 之外的地方 ——
# 名单是这个项目里最敏感的文件，不该赌「配置永远不挪窝」。
# 名单导出源文件也显式写一条：它已经从项目根挪到 secrets/ 了，
# 但 ROSTER_DB_PATH 可以被配到任何地方，不该赌「它永远在 secrets/ 里」。
accounts.jsonl
roster.db

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# 数据库文件
*.db

# 运行期数据目录：SQLite 库（主库 / 名单库）、迁移前自动生成的 .db.bak-* 备份、
# 以及学生上传的打印件，全在这里，都不该进仓库 ——
# 尤其名单库，里面是几万人的真实姓名 + 学号，推上公开仓库收不回来。
#
# 这里按**整个目录**忽略，而不是逐个文件名：以后往 data/ 下加新库或新子目录，
# 不用记得回来补规则。漏一次，推上去的就是别人的个人信息。
# 注意 *.db 挡不住 .db.bak-* 这种备份名（结尾不是 .db），所以必须整目录忽略。
data/

# 退役的 QQ 框架与安装包（NapCat 两套、旧版 LLBot、三个上游 zip，合计约 300MB）。
# 留着只是万一要翻历史，它们跟这个项目能不能跑已经没关系了 —— 整个目录可以随时删。
vendor/_retired/

# .md 文档不提交
*.md

# 例外：这两份 README 要留着
!README.md
!vendor/llonebot/README.md

# 运行日志里有客户端 IP、账号和操作记录，算隐私数据，不要提交到公开仓库。
#
# 2026-09-22 起日志默认落在 `<DATA_DIR>/logs`（也就是 data/logs），已经被上面的 data/ 规则挡住；
# 这条 `logs/` 留着是为了兜住两件事：① 老部署升级前日志还在项目根；② 有人显式把 LOG_DIR
# 配回项目根。`*.log` 那两条则是兜 printbot 与 LLBot 各自的日志目录。
logs/
*.log
*.log.*

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.local
.env.production
.env.*.local
!.env.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# 业务数据库文件（属于运行时数据，不提交仓库）
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# 上传的文件（运行时数据，且含学生提交的内容，不提交仓库）
print_files/

# ---------- 前端（frontend/） ----------
frontend/node_modules/
frontend/.vite/

# 构建产物 static/app/ 是**刻意入库**的：部署机上通常只有 Python 环境，
# 产物跟着仓库走就不用为了发个版本去装 Node。改完前端记得
# npm run build 并把 static/app/ 一并提交，否则线上还是旧界面。

# 这里曾有一行 `!AGENTS.md`，给 AI 助手的说明文件开白名单。
# 现已取消：AGENTS.md 属于个人本地工具配置（里面写了本机端口占用、
# 历史 bug 现场这类只对当前开发机有意义的内容），不该进公开仓库。
# 所以它重新落回上面的 `*.md` 规则里，文件仍留在磁盘上，只是不再被跟踪。
# 若哪天确认要团队共享，把 `!AGENTS.md` 加回本行位置即可。

# NapCat 启动器每次启动都会在**当前工作目录**生成一个加载器（内容是指向
# napcat.mjs 的绝对路径）—— 本机运行产物，换台机器路径就不对，不该进仓库。
# NapCat 已经退役，那个文件连同整套东西都在 vendor/_retired/ 下了；
# 这条规则留着是因为它的落点跟着**当前工作目录**走，哪天有人在根上再起一次还会生成。
loadNapCat.js

# Cursor  
#  Cursor is an AI-powered code editor.`.cursorignore` specifies files/directories to 
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore
